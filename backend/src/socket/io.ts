import { Server } from 'socket.io';

// This file will house shared socket utilities and initialization
export const initializeSocket = (io: Server) => {
  // Initialize socket configuration here if needed
  return io;
};